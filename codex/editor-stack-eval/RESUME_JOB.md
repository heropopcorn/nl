# 续传：按线框重构导演台功能区布局（Layout V3）

你上一轮已完成技术栈评估（EVALUATION.md），结论是继续全 Godot、继续做可视化编辑器。
用户现在给出 UI 分区线框图与详细需求，请在同一产品路线下执行。

## 用户原话
见 USER_LAYOUT_V3.md（必须原样理解，勿改写意图）。
附图：layout-wireframe.png（布局分区示意）。

## 交付要求
1. 先写设计/需求文档：
   `codex/editor-stack-eval/LAYOUT_V3_REQUIREMENTS.md`
   对照线框标注：元素列表 / 资源列表 / 工具 / 主编辑区 / 属性区 / 底栏章节场景 / 顶栏文件编辑；
   写清与现有 V2 的映射、数据模型变化（一切皆元素）、P0 切片与可后做项。
2. 然后在仓库实现 Layout V3 的 UI 壳与主交互（分支如 `codex/director-desk-layout-v3`）：
   - 按图重排功能区
   - Hierarchy：当前场景一切皆元素（背景、角色、水流、特效、底图裁片区域等）；无场景时空白或提示打开场景；点击选中并在主编辑区高亮
   - 资源列表：默认资源 / 自定义资源 tabs；分类 背景图/树木/角色/房屋；点击放置；背景唯一，已有则确认是否替换；自定义支持上传
   - 属性面板：随选中元素变化；图层；人物/树木等可加路线（速度、显示）；区域类可多个范围（水流、雾气预留）；背景可看到其下裁片区域列表
   - 底栏 Project：左树右文件夹式；章 → 场景
   - 工具条：移动、缩放、旋转、镜像（至少做出可用骨架，未实现的能力可灰显并写明）
   - 顶栏：文件、编辑；文件可新建章节/场景/元素
3. 尽量复用现有控制器与 scene schema；需要扩展 schema 时保持向后兼容与自测。
4. 跑相关 Godot selftest；提交、推送、开 PR。最终 lastmsg 给中文摘要 + PR 链接。

约束：全 Godot；中文 UI；不要 JS 重写编辑器；不要无关大重构。
